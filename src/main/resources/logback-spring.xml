<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="LOG_FILE_PATH" value="${LOG_FILE_PATH:-../logs}"/>
    <property name="LOG_NAME" value="${LOG_NAME:-document-service}"/>

    <!-- Base appenders -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level [traceId=%X{traceId:-}, spanId=%X{spanId:-}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE_PATH}/${LOG_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE_PATH}/${LOG_NAME}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>300MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level [traceId=%X{traceId:-}, spanId=%X{spanId:-}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Themed appenders for io.aurasage package -->
    <appender name="AURASAGE_CONSOLE" class="io.github.h3yzack.vivid4jlog.appender.Vivid4jLogAppender">
        <theme>CLASSIC</theme>
        <colorEnabled>true</colorEnabled>
        <appender-ref ref="CONSOLE"/>
    </appender>

    <appender name="AURASAGE_FILE" class="io.github.h3yzack.vivid4jlog.appender.Vivid4jLogAppender">
        <theme>CLASSIC</theme>
        <colorEnabled>false</colorEnabled> 
        <appender-ref ref="FILE"/>
    </appender>


    <!-- JSON file appender (all packages) -->
    <appender name="JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE_PATH}/${LOG_NAME}.json</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE_PATH}/${LOG_NAME}.%d{yyyy-MM-dd}.json</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp/>
                <logLevel/>
                <loggerName/>
                <message/>
                <mdc/>
                <arguments/>
                <stackTrace/>
            </providers>
        </encoder>
    </appender>
    
    <!-- Default root logger (plain, no themes) -->
    <root level="ERROR">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Package-specific loggers -->
    <logger name="io.aurasage" level="DEBUG" additivity="false">
        <appender-ref ref="AURASAGE_CONSOLE"/>
        <appender-ref ref="AURASAGE_FILE"/>
        <appender-ref ref="JSON_FILE"/>
    </logger>
    
</configuration>